[package]
name = "compiler"
version = "0.1.0"
edition = "2024"

[dependencies]
scanner = { path = "../scanner" }
miette = { workspace = true, features = ["fancy"] }

[dev-dependencies]
test-case = "3.3.1"

[features]
disassembly = []
printcode = []